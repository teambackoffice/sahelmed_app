import 'package:flutter/material.dart';

class CreateMachineCertificate extends StatelessWidget {
  const CreateMachineCertificate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Machine Certificate')),
      body: const Center(child: Text('Create Machine Certificate')),
    );
  }
}
