import 'package:flutter/material.dart';

class SalesEngineerHomepage extends StatelessWidget {
  const SalesEngineerHomepage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Sales Person Homepage')));
  }
}
